name: blurry
description: A new flutter dialog display with blurry effect and built-in themes.
version: 1.4.0
homepage: https://github.com/koukibadr/Blurry

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:

